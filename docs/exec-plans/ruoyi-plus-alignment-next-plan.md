# RuoYi-Vue-Plus 对齐后续计划

- 已具备：用户、角色、菜单、部门、岗位、字典、参数、日志、在线用户、任务、文件和工作台。
- 部分具备：监控、文件存储、数据权限、限流。
- P1：客户端管理、文件配置、Redis 监控、demo 模块。
- P2：租户、字段加密、安全增强、监控增强和 Docker Compose。
- P3：WebSocket/SSE、邮件短信、代码生成和 AI 助手。
- 不在近期范围：OAuth/OIDC 和分布式任务。

P0 先保证 clone、安装、测试与文档。每项能力必须同时包含后端、前端、SQL、权限、测试和文档。

